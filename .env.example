# ============================================
# PresuSimple Mobile - Environment Variables
# ============================================
# 
# SETUP:
# 1. Copy this file to .env.local
# 2. Fill in your actual Google OAuth credentials
# 3. Run with: npm run start:dev | start:staging | start:prod
#
# NOTE: API URLs are configured in app.config.ts based on environment.
# You only need .env.local for secrets (Google OAuth).
# ============================================

# Google OAuth Credentials (REQUIRED for Google Sign-In)
# Get these from: https://console.cloud.google.com/apis/credentials
EXPO_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id
EXPO_PUBLIC_GOOGLE_CLIENT_SECRET=your-google-client-secret
EXPO_PUBLIC_GOOGLE_EXPO_CLIENT_ID=your-expo-google-client-id
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your-ios-google-client-id
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your-android-google-client-id
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-web-google-client-id

# ============================================
# OPTIONAL OVERRIDES
# These override the defaults in app.config.ts
# Usually not needed - just use npm run start:prod
# ============================================

# Override API URL (optional - defaults based on environment)
# EXPO_PUBLIC_API_BASE_URL=https://custom-api.example.com

# Override Feature Flags URL (optional - auto-derived from API URL)
# EXPO_PUBLIC_FEATURE_FLAGS_URL=https://custom-api.example.com/api/feature-flags
